# Normalize line endings across platforms.
# The repository always stores text files with LF; Git converts to the
# platform's convention in the working tree on checkout.
* text=auto eol=lf

# Binary files Git must never touch or normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
